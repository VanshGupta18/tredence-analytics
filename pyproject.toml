[project]
name = "tredence-analytics"
version = "0.1.0"
description = "case-study"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.0.0",
    "torchvision>=0.15.0",
    "matplotlib>=3.7.0",
]
